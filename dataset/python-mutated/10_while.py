def readline(self, line):
    if False:
        i = 10
        return i + 15
    while True:
        if self.join_lines:
            if line:
                continue
            if self:
                continue
        return line
while __name__ != '__main__':
    b = 4