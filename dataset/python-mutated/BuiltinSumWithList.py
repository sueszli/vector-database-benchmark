import itertools
empty = ()
value = list(range(10))

def calledRepeatedly(iterable, empty):
    if False:
        i = 10
        return i + 15
    value
    y = sum(iterable)
    y = sum(empty)
    return (x, y)
for x in itertools.repeat(None, 50000):
    calledRepeatedly(value, empty)
print('OK.')