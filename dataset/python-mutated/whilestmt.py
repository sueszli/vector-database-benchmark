def whilestmt(self, lhs: str, n: int, rule, tree, tokens: list, first: int, last: int) -> bool:
    if False:
        while True:
            i = 10
    return tokens[last - 1] == 'POP_BLOCK' and tokens[last] not in ('JUMP_FORWARD', 'COME_FROM_LOOP', 'COME_FROM')