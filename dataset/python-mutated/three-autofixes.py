import requests

def _do_stuff():
    if False:
        for i in range(10):
            print('nop')
    return requests.post(headers=auth_headers, url=self.url, endpoints='endpoint')

def _do_other_stuff():
    if False:
        while True:
            i = 10
    return requests.post(headers=auth_headers, url=self.url, endpoints='endpoint')

def _do_other_stuff2():
    if False:
        print('Hello World!')
    return requests.post(headers=auth_headers, url=self.url, endpoints='endpoint')